export const setPoke = (data) => {
  return (dispatch, getState) => {
    let action = {};
    dispatch(action);
  };
};

export const getPoke = (data) => {
  return (dispatch, getState) => {
    let action = {};
    dispatch(action);
  };
};
